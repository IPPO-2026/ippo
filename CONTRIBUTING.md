# 함께 개발하기

Node.js 24 LTS와 `npm ci`를 사용합니다. `npm run dev`는 브라우저 체험 모드로도 동작하며, API를 함께 확인할 때는 `npm run build` 후 별도 터미널에서 `npm run dev:api`를 실행합니다.

1. [로드맵](docs/roadmap.md)과 관련 이슈에서 사용자 문제·완료 조건을 확인합니다.
2. `codex/<작업명>` 또는 팀에서 합의한 기능 브랜치에서 작업합니다.
3. 한국어·일본어 문구, 실패 상태, 동의·삭제 동작을 함께 수정합니다.
4. `npm run check`를 실행하고 PR에 실제 검증 결과를 남깁니다.
5. 리뷰 후 `main`으로 병합합니다. 클라우드 배포는 별도 단계입니다.

실제 대화, 비밀키, 개인정보, `.dev.vars`, `wrangler.live.jsonc`, 로컬 저장 데이터는 커밋하지 않습니다. 사용자 식별이 가능한 화면 캡처도 공개 이슈에 올리지 않습니다. 오류 재현에는 합성 문장을 사용합니다.

AI 모델/프롬프트 변경은 한·일 평가 시나리오와 무료 한도 추정을 같이 갱신합니다. UI 언어와 도움 지역은 별개입니다. 서버 저장·기관 공유·미성년자 대상 서비스를 추가할 때는 기존 체험 범위를 넘어서는 변경임을 명시합니다.

라이선스는 아직 선정하지 않았습니다. 공개 열람과 재배포·상업 이용 허가는 별개이며, 도입 전 권리자와 라이선스를 정해야 합니다.
